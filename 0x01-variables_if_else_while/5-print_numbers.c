#include <stdio.h>

/**
 * main - main entry point
 * Description:prints the numbers
 * Return:0(success)
*/

int main(void)
{
	int number;

	for (number = 0; number <= 10; number++)
	{
		printf("%d", number);
	}
	printf("\n");
	return (0);
}
